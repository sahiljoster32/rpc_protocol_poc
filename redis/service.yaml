apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379      # Service port
      targetPort: 6379  # Container port
  type: NodePort  # Exposes Redis on a node port (for external access)
